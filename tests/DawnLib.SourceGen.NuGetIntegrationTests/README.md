# NuGet Integration Tests

This project mirrors `DawnLib.SourceGen.IntegrationTests`, but builds with
NuGet packages of DawnLib instead of references, and for that reason is not
part of the MSBuild Solution structure.

The script `test.ps1` gives an idea how to manually run this test.
